module.exports = {
    
    stub: (server) => {
     /*   server.app.get('/todo',(req, resp) => {
            
            const todoList = require('../../data/todo.json');
            resp.send(todoList);
        })
        server.app.post('/todo',(req, resp) => {
            
            resp.send({hello: 'Hello'});
        })
        server.app.get('/task',(req,resp)=> {
            const taskList = require('../../data/todo.json');
            resp.send(taskList);
        })*/
    }
}